---
title: "Authentication"
description: "Authenticate API requests using API keys."
---

The API uses token-based authentication. All requests must include an API key in the `Authorization` header using the Bearer token format:

```http
Authorization: Bearer <your_api_key>
```

API keys are organization-scoped. All resources (organization, verifications, checks, webhooks) belong to your organization and are scoped automatically through the header.

To create an API key:
- **Dashboard:** [app.ezyshield.com.au](https://app.ezyshield.com.au) → Organization settings
- **API:** Once you have your first key, create additional keys with [List all API keys](/api-reference/api-keys/list-all-api-keys) and [Create an API key](/api-reference/api-keys/create-an-api-key)
